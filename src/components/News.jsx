import React from 'react';
import { Select,Col,Row,Typography,Card,Avatar } from 'antd';
import moment from 'moment';
import {useGetCryptoNewsQuery} from '../services/cryptoNewsApi';

const[Text,Title] = Typography;
const [Option] = Select;

const News = () => {
  return (
    <div>
      News
    </div>
  )
}

export default News
